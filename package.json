{
  "author": {
    "name": "Neski",
    "url": "https://twitter.com/itsneski"
  },
  "bin": {
    "jet": "./jet"
  },
  "bugs": {
    "url": "https://github.com/itsneski/lightning-jet/issues"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.6.8",
    "@grpc/proto-loader": "^0.7.0",
    "balanceofsatoshis": "^19.4.10",
    "commander": "^14.0.3",
    "date-and-time": "^2.0.1",
    "deasync": "0.1.28",
    "find-process": "^1.4.5",
    "import-lazy": "^4.0.0",
    "node-os-utils": "^1.3.7",
    "node-telegram-bot-api": "^0.66.0",
    "run-parallel": "1.2.0",
    "sqlite3": "^5.1.2",
    "winston": "^3.10.0"
  },
  "description": "Lightning Jet is a fully automated rebalancer for Lightning nodes. It optimizes channel liquidity allocation based on routing volume, missed routing opportunities (HTLCs), and other variables.",
  "engines": {
    "node": ">=12"
  },
  "keywords": [
    "lightning",
    "lightning network",
    "lnd",
    "rebalance",
    "circular rebalance",
    "rebalancer",
    "automated rebalancer"
  ],
  "license": "MIT",
  "main": "jet",
  "name": "lightning-jet",
  "repository": {
    "type": "git",
    "url": "https://github.com/itsneski/lightning-jet.git"
  },
  "scripts": {
    "postinstall": "./tools/genconfig",
    "check:cli": "node jet --help && node jet status --help && node jet info --help && node jet stats --help && node jet probes --help && node jet rebalance --help && node jet pay --help && node jet peers --help && node jet fees --help && node jet analyze-fees --help && node jet htlc-history --help && node jet htlc-analyzer --help && node jet rebalance-history --help && node jet list-peers --help && node jet list-channels --help && node jet monitor --help && node jet fee-history --help && node jet channel-events --help && node jet channeldb --help && node jet send-message --help && node jet update-channel --help && node jet close-channel --help && node jet reconnect --help"
  },
  "version": "1.6.0"
}
